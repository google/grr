#!/bin/sh
#
# Pre installation script for GRR client MacOS-X package

if [ -f "%(ClientBuilder.plist_path)" ];
then
  sudo launchctl unload "%(ClientBuilder.plist_path)";
  sudo rm -f "%(ClientBuilder.plist_path)";
fi

